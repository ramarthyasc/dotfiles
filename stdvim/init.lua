-- The folder '~/.config/stdvim' is the one required by nvim 
-- (using lua runtime) - which automatically runs this init.lua file 
-- (special file like in python)
--
-- requires from a lua folder in this director
require("amarthya")
